export interface Input {
  accessToken: string;
  apiKey: string;
  model: string;
  maxIssues: number;
  minSimilarity: number;
  discussions: boolean;
  showSimilarity: boolean;
  label: string;
  template: string;
}
